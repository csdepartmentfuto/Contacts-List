<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.contacts_list">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ContactsList">
        <activity
            android:name=".WorkActivity"
            android:label="@string/work_category_text" />
        <activity
            android:name=".SchoolActivity"
            android:label="@string/school_category_text" />
        <activity
            android:name=".FriendsActivity"
            android:label="@string/friends_category_text" />
        <activity
            android:name=".FamilyActivity"
            android:label="@string/family_category_text" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>